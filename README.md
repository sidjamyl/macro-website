# e-tqan mobile prototype

Expo React Native demo for the student-facing e-tqan platform.

## Run locally

```bash
npm install
npm run server
```

In another terminal:

```bash
npm run web
```

The frontend stays browsable with demo data if the API is offline. Start the API to persist appointments, maintenance requests, event registrations and simulated SOS alerts.
